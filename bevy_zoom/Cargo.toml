[package]
name = "bevy_zoom"
version = "0.1.0"
edition = "2024"
license-file.workspace = true
repository.workspace = true

[dependencies]
bevy = { version = "0.19.0", default-features = false }
bevy_drag = { version = "0.1.0", path = "../bevy_drag" }
query_ext = { version = "0.1.0", path = "../query_ext" }
